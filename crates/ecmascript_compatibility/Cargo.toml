[package]
name = "ecmascript_compatibility"
version = "0.1.0"
edition = "2024"

[dependencies]
oxc = { workspace = true, features = ["ast_visit"] }
browserslist-rs={workspace = true}
thiserror={workspace = true}
